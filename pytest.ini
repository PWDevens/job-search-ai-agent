[pytest]
# pytest_asyncio 1.4.0 + pytest 9.x breaks the built-in tmp_path fixture
# ('WindowsPath' has no attribute 'mktemp') at fixture setup. The suite has NO async
# tests, so disable the unused plugin rather than pin versions.
addopts = -p no:asyncio
testpaths = tests
